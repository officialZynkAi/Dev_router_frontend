<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ZYNK</title>

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/github.min.css" rel="stylesheet">

<style>
*{margin:0;padding:0;box-sizing:border-box;font-family:'Inter',sans-serif}

body{
  background:#ffffff;
  height:100vh;
  overflow:hidden;
}

/* ===== LAYOUT ===== */

.wrapper{
  display:flex;
  height:100vh;
}

/* ===== SIDEBAR ===== */

.sidebar{
  width:260px;
  background:#ffffff;
  border-right:1px solid #f0f0f0;
  padding:20px;
  display:flex;
  flex-direction:column;
  transition:0.3s;
  position:relative;
}

.sidebar h2{
  font-size:20px;
  margin-bottom:20px;
  letter-spacing:1px;
}

.collapse-btn{
  position:absolute;
  top:20px;
  right:-12px;
  background:#111827;
  color:white;
  width:24px;
  height:24px;
  border-radius:50%;
  display:flex;
  align-items:center;
  justify-content:center;
  cursor:pointer;
  font-size:12px;
}

.new-chat{
  padding:10px;
  border-radius:12px;
  border:1px solid #e5e5e5;
  background:white;
  cursor:pointer;
  margin-bottom:15px;
  font-weight:500;
}

.chat-list{
  flex:1;
  overflow-y:auto;
}

.chat-item{
  padding:8px;
  border-radius:8px;
  cursor:pointer;
  font-size:14px;
}

.chat-item:hover{
  background:#f5f5f5;
}

.logout{
  margin-top:10px;
  padding:8px;
  border-radius:8px;
  border:1px solid #e5e5e5;
  background:white;
  cursor:pointer;
}

/* ===== CHAT AREA ===== */

.chat-area{
  flex:1;
  display:flex;
  flex-direction:column;
  align-items:center;
}

.topbar{
  width:100%;
  height:60px;
  border-bottom:1px solid #f0f0f0;
  display:flex;
  align-items:center;
  padding:0 20px;
  font-weight:600;
  letter-spacing:2px;
}

.menu-btn{
  display:none;
  font-size:22px;
  cursor:pointer;
  margin-right:15px;
}

.chat-container{
  flex:1;
  width:100%;
  overflow-y:auto;
  display:flex;
  justify-content:center;
  padding:30px 20px 120px 20px;
}

.chat{
  width:100%;
  max-width:820px;
  display:flex;
  flex-direction:column;
  gap:24px;
}

/* ===== MESSAGE ===== */

.message{
  display:flex;
}

.message.user{
  justify-content:flex-end;
}

.bubble{
  max-width:85%;
  padding:16px 20px;
  border-radius:20px;
  line-height:1.6;
  font-size:15px;
  box-shadow:0 4px 16px rgba(0,0,0,0.04);
  word-wrap:break-word;
}

.message.user .bubble{
  background:#111827;
  color:white;
}

.message.ai .bubble{
  background:#f9fafb;
  color:#111;
}

/* ===== THINKING ===== */

.thinking{
  font-size:14px;
  opacity:0.6;
}

.dots span{
  animation:blink 1.4s infinite both;
}

.dots span:nth-child(2){animation-delay:0.2s}
.dots span:nth-child(3){animation-delay:0.4s}

@keyframes blink{
  0%{opacity:.2}
  20%{opacity:1}
  100%{opacity:.2}
}

/* ===== CODE ===== */

pre{
  position:relative;
  margin-top:10px;
  border-radius:12px;
  overflow:hidden;
}

.copy-btn{
  position:absolute;
  top:8px;
  right:8px;
  background:white;
  border:1px solid #ddd;
  border-radius:8px;
  padding:4px 8px;
  font-size:12px;
  cursor:pointer;
}

/* ===== INPUT ===== */

.input-wrapper{
  position:fixed;
  bottom:0;
  width:100%;
  background:white;
  border-top:1px solid #f0f0f0;
  padding:15px;
  display:flex;
  justify-content:center;
}

.input-box{
  width:100%;
  max-width:820px;
  display:flex;
  gap:10px;
}

textarea{
  flex:1;
  resize:none;
  padding:14px;
  border-radius:16px;
  border:1px solid #e5e5e5;
  font-size:14px;
  outline:none;
  max-height:200px;
  overflow-y:auto;
}

.send-btn{
  width:48px;
  height:48px;
  border-radius:50%;
  border:none;
  background:#111827;
  color:white;
  cursor:pointer;
}

/* ===== MOBILE ===== */

@media(max-width:900px){
  .sidebar{
    position:fixed;
    left:-260px;
    top:0;
    height:100%;
    z-index:1000;
  }
  .sidebar.open{
    left:0;
  }
  .menu-btn{
    display:block;
  }
}
</style>
</head>

<body>

<div class="wrapper">

<div class="sidebar" id="sidebar">
  <h2>ZYNK</h2>
  <div class="collapse-btn" onclick="toggleSidebar()">‹</div>
  <button class="new-chat" onclick="createChat()">+ New Chat</button>
  <div class="chat-list" id="chatList"></div>
  <button class="logout" onclick="logout()">Logout</button>
</div>

<div class="chat-area">

<div class="topbar">
  <div class="menu-btn" onclick="toggleSidebar()">☰</div>
  ZYNK
</div>

<div class="chat-container">
  <div class="chat" id="chat"></div>
</div>

<div class="input-wrapper">
  <div class="input-box">
    <textarea id="input" rows="1" placeholder="Message Zynk..."></textarea>
    <button class="send-btn" onclick="send()">➤</button>
  </div>
</div>

</div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/highlight.min.js"></script>

<script>
const backend="https://dev-router.onrender.com";
const token=localStorage.getItem("token");

let currentChatId=null;
let isStreaming=false;

function toggleSidebar(){
  document.getElementById("sidebar").classList.toggle("open");
}

function logout(){
  localStorage.removeItem("token");
  location.href="index.html";
}

function autoResize(){
  const input=document.getElementById("input");
  input.style.height="auto";
  input.style.height=input.scrollHeight+"px";
}

document.getElementById("input").addEventListener("input",autoResize);

function format(text){
  text=text.replace(/```([\s\S]*?)```/g,(match,code)=>{
    return `<pre><button class="copy-btn" onclick="copyCode(this)">Copy</button><code>${code.replace(/</g,"&lt;")}</code></pre>`;
  });
  return text.replace(/\n/g,"<br>");
}

function copyCode(btn){
  const code=btn.parentElement.querySelector("code").innerText;
  navigator.clipboard.writeText(code);
  btn.innerText="Copied";
  setTimeout(()=>btn.innerText="Copy",1000);
}

function addMessage(text,role){
  const chat=document.getElementById("chat");
  const row=document.createElement("div");
  row.className="message "+role;
  const bubble=document.createElement("div");
  bubble.className="bubble";
  bubble.innerHTML=text;
  row.appendChild(bubble);
  chat.appendChild(row);
  chat.scrollTop=chat.scrollHeight;
  document.querySelectorAll("pre code").forEach(block=>hljs.highlightElement(block));
  return bubble;
}

async function send(){
  if(isStreaming) return;

  const input=document.getElementById("input");
  const q=input.value.trim();
  if(!q) return;

  isStreaming=true;
  addMessage(format(q),"user");
  input.value="";
  autoResize();

  const bubble=addMessage(`<div class="thinking">Thinking<span class="dots"><span>.</span><span>.</span><span>.</span></span></div>`,"ai");

  const res=await fetch(`${backend}/ask`,{
    method:"POST",
    headers:{
      "Content-Type":"application/json",
      "Authorization":"Bearer "+token
    },
    body:JSON.stringify({question:q,chat_id:currentChatId})
  });

  const reader=res.body.getReader();
  const decoder=new TextDecoder();
  let fullText="";

  while(true){
    const {done,value}=await reader.read();
    if(done) break;

    const chunk=decoder.decode(value);
    const lines=chunk.split("\n\n");

    for(let line of lines){
      if(line.startsWith("data: ")){
        const raw=line.replace("data: ","").trim();
        if(raw==="[DONE]"){
          bubble.innerHTML=format(fullText);
          isStreaming=false;
          return;
        }

        try{
          const json=JSON.parse(raw);
          if(json.type==="token"){
            fullText+=json.value;
            bubble.innerText=fullText;
          }
        }catch{
          fullText+=raw;
          bubble.innerText=fullText;
        }
      }
    }
  }

  bubble.innerHTML=format(fullText);
  isStreaming=false;
}
</script>

</body>
</html>
